import React, { useState } from 'react';
import { createPortal } from 'react-dom';

function BackBack() {
    return <button className='btn btn-warning absolute left-4 top-4'>HOME</button>;
}

export default BackBack;